# Changelog

## Unreleased

## 1.4.7

- 设置逻辑从主窗口进一步抽离，设置页保留为独立窗口，减少 App 壳层重复状态与重复实现
- 新增每日回顾 / AI 洞察工作流，支持流式生成、快照保存与回顾窗口
- 外部接入补齐通用 CLI / Skill 接入包，并完善相关设置、状态检查与接入说明
- JSON 完整备份升级为内容 + 附件 + 设置快照，导入预览会明确提示是否包含设置
- 优化时间轴、日历、连接图、笔记本、文档快照与数据管理等界面的结构与加载体验

## 1.4.5

- 设置改为独立窗口，重做为更小尺寸的双栏布局，并新增高级设置、备份与加载备份入口
- 新增数据管理页，支持按天浏览记录、批量删除块、查看数据量统计、打开数据目录与设置目录
- 把运行与索引状态收口到设置页，同时补上 session / lifetime token 使用统计
- 优化日历、笔记本与文档快照页面，减少卡片套层，压缩无用标题文案并修复窄窗口布局问题
- 优化主界面切页加载路径，减少笔记本、日历、搜索、快照等页面的首屏 loading 闪烁
- 修复 macOS 关闭主窗后仅剩设置窗时无法恢复主窗、设置页目录打开失败无提示、数据管理刷新不完整等问题

## 1.4.1

- 修复安装版 `sqlite-vec` 扩展加载路径，改为优先使用 `app.asar.unpacked` 中的动态库
- 修复安装版向量索引未初始化导致的向量补齐积压与“查找相关块失败”
- 补充打包路径回归测试，覆盖原生扩展在 Electron 打包场景下的加载行为

## 1.3.1

- 修复时间轴输入框图片粘贴与拖拽失效，兼容 clipboard `items`、`files` 和 Finder 拖入场景
- 修复编辑态图片 Markdown 仅显示链接的问题，输入框现在会以内嵌图片方式预览
- 为本地附件引入受控的 `changbu-attachment://` 协议，避免渲染层直接加载 `file://` 图片失败
- 补充本地附件 URL、Markdown 图片解析与图片传输提取相关测试

## 1.3.0

- 向量补齐改为持久化队列 + 后台批处理 drain，reindex 运行期间新入队任务也会继续处理到队列清空
- 修复块 enrich 异步竞争：旧任务不会再覆盖较新的标签、摘要或错误状态，删除后的块也不会被晚到任务写回
- 搜索结果与 notebook 侧边栏补充标签 / 全文 / 向量命中来源，并转义 LIKE 特殊字符以避免 `%`、`_` 查询误伤
- 修复退出应用时等待后台任务完成、设置失败提示、快照选择、时间轴索引、分页合并与笔记本加载状态等前端与主进程细节
- 同步 README 与 CLAUDE 文档到“标签/摘要即时补全，向量后台批处理补齐”的当前实现

## 0.1.0

- 初始化 Electron + React + TypeScript + Vite 桌面应用结构
- 接入 SQLite、FTS5、sqlite-vec 与向量维度自适应
- 实现块创建、编辑、删除、标签系统与按标签浏览
- 接入 OpenAI 兼容 Embedding / LLM API，并验证硅基流动兼容性
- 支持文档流式生成与 Markdown 阅读态展示
- 支持 Markdown 渲染、图片粘贴、本地附件保存与附件表关联
- 补齐时间轴历史懒加载、搜索入口形态、搜索高亮等 PRD 体验缺口
